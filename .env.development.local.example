# 本地开发环境配置示例
# 复制此文件为 .env.development.local 并根据实际情况修改
# 此文件可以提交到版本控制，但 .env.development.local 不会被提交

# ========================================
# API 配置（二选一）
# ========================================

# 方式A: 完整URL配置（推荐）
# Windows 用户示例 - 替换为你的实际IP地址
# VITE_API_BASE_URL=http://192.168.1.100:3000

# Mac/Linux 用户示例 - 监听所有网络接口
# VITE_API_BASE_URL=http://0.0.0.0:3000

# 本地开发默认配置
VITE_API_BASE_URL=http://localhost:3000

# 方式B: 分组配置（当未设置 VITE_API_BASE_URL 时使用）
# VITE_API_PROTOCOL=http
# VITE_API_HOST=localhost         # Windows: 改为你的IP，如 192.168.1.100
# VITE_API_PORT=3000

# ========================================
# 文件存储配置
# ========================================

# 存储目录配置（相对路径，自动创建）
VITE_STORAGE_UPLOADS_DIR=./uploads     # 用户上传的文件存储目录
VITE_STORAGE_DATA_DIR=./data          # 应用数据和数据库文件目录
VITE_STORAGE_TEMP_DIR=./temp          # 临时文件目录（预留）

# ========================================
# 开发调试配置
# ========================================

# 启用调试模式
VITE_ENABLE_DEBUG=true
VITE_ENABLE_CONSOLE_LOG=true

# 启用模拟数据（本地模式时使用）
VITE_ENABLE_MOCK=true

# ========================================
# 文件上传配置
# ========================================

# 文件大小限制（字节）
VITE_MAX_FILE_SIZE=52428800           # 50MB

# 支持的文件类型
VITE_ALLOWED_IMAGE_TYPES=jpg,jpeg,png,gif,webp
VITE_ALLOWED_VIDEO_TYPES=mp4,mov,avi,mkv,webm

# API 请求超时时间（毫秒）
VITE_API_TIMEOUT=30000

# ========================================
# 使用说明
# ========================================

# 1. Windows 用户：
#    - 取消注释 VITE_API_BASE_URL，设置为你的局域网IP
#    - 例如：VITE_API_BASE_URL=http://192.168.1.100:3000
#
# 2. Mac/Linux 用户：
#    - 设置 VITE_API_BASE_URL=http://0.0.0.0:3000
#    - 或保持默认的 localhost
#
# 3. 获取你的IP地址：
#    Windows: ipconfig | findstr "IPv4"
#    Mac/Linux: ifconfig | grep "inet " | grep -v 127.0.0.1
#
# 4. 测试配置：
#    - 启动项目：npm run dev:h5:local
#    - 本机访问：http://localhost:3000
#    - 局域网访问：http://你的IP:3000